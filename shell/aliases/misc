#############################
# alias
#############################
alias ll='ls -l'
alias lla='ls -la'

# find file recursive
fr(){
  find . -name $1 -print
}

# parse json
json(){
  echo $1 | python -m json.tool
}
